VITE_API_BASE_URL=http://localhost:8080
# Razorpay Key Id (Dashboard → API Keys → Key Id). Must match Spring `razorpay.key` — NOT a placeholder.
# Using rzp_test_example causes 401 on api.razorpay.com and a broken checkout UI.
VITE_RAZORPAY_KEY=
